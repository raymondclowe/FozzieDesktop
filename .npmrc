# Suppress funding and audit messages to reduce noise during installs
fund=false
audit=false

# Note: The git dependency integrity warning for @electron/node-gyp is expected
# and comes from Electron dependencies. This is a known npm behavior for git dependencies.